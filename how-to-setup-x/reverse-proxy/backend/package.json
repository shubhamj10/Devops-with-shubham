{
  "name": "reverse-proxy",
  "version": "1.0.0",
  "description": "This guide sets up a **Reverse Proxy** on your local machine using **NGINX** running on Ubuntu (WSL). We'll route incoming requests from port `80` to a backend server running on another port (e.g., `3000`).",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^5.1.0"
  }
}